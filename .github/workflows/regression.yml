name: Run Regression Tests

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  test-and-summary:
    uses: ./.github/workflows/reusable.yml
    with:
      bun-version: latest

  print-summary:
    needs: test-and-summary
    runs-on: ubuntu-latest
    steps:
      - name: 📝 Print Test Summary
        run: |
          echo "Smoke Test Results:"
          echo "1. E2E test results 1: ${{ needs.test-and-summary.outputs.test_result_1 == 'success' && '✅' || '❌' }}"
          echo "2. ID test results 2: ${{ needs.test-and-summary.outputs.test_result_2 == 'success' && '✅' || '❌' }}"
          echo "3. POST test results 3: ${{ needs.test-and-summary.outputs.test_result_3 == 'success' && '✅' || '❌' }}"