name: Run Regression Tests

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  test-and-summary:
    uses: ./.github/workflows/reusable.yml
    with:
      bun-version: latest

    # This allows us to run steps after the reusable workflow
    secrets: inherit

  print-summary:
    needs: test-and-summary
    runs-on: ubuntu-latest
    steps:
      - name: 📝 Print Test Summary
        run: |
          echo "Smoke Test Results:"
          echo "1. Test 1: ${{ needs.test-and-summary.outputs.test_result == 'success' && '✅' || '❌' }}"